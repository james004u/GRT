// Simulate sending email with account details
function sendPasswordEmail(email) {
    // Placeholder function for actual email sending logic
    console.log(`Password has been sent to ${email}`);
}

// Simulate email sending for demo purposes
document.addEventListener('DOMContentLoaded', function () {
    const userEmail = 'yourmail@hotmail.com';
    sendPasswordEmail(userEmail);
});
